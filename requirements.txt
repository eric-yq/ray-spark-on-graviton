# Benchmark dependency lock — IDENTICAL across m7i (x86_64) and m8g (aarch64).
# Only the CPU architecture differs between the two clusters; every version below
# is held constant so the comparison isolates the hardware.
#
# Compatibility anchor: RayDP 1.6.5 (latest, 2026-03) requires
#   ray>=2.37.0 , pyspark>=3.1.1,<=3.5.7 , pyarrow>=4.0.1 , protobuf>3.19.5
# All of these resolve cleanly on both manylinux x86_64 and aarch64 wheels.

# --- Core engines (exact pins: interop-sensitive) ---
ray[default,data]==2.44.1
raydp==1.6.5
pyspark==3.5.7          # pip bundle ships Spark + Hadoop 3.3.4 client (no separate Spark install)
pyarrow==17.0.0
pandas==2.2.3
numpy==1.26.4           # <2.0 to avoid ABI churn with pinned pyarrow/pandas

# --- Tooling (compatible-release pins) ---
tpchgen-cli>=3.0,<4     # pure-Rust TPC-H parquet generator (x86_64 + aarch64 wheels)
duckdb>=1.1,<2          # optional: local TPC-H gen / ad-hoc parquet inspection
boto3>=1.35,<2          # S3 upload / pricing refresh
s3fs>=2024.6            # fsspec S3 fallback for Ray Data
psutil>=6.0             # per-node resource sampling
